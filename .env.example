# Backend API base URL. Must be NEXT_PUBLIC_ (not just API_URL) because
# this app is fully client-rendered - every API call happens in the
# browser, not in a Next.js Server Component.
NEXT_PUBLIC_API_URL=https://139-59-232-15.sslip.io
